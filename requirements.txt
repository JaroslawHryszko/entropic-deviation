# Core libraries
torch                 # GPU build, CUDA 12.x
numpy
pandas
scipy
statsmodels           # AR(1) & OLS
psutil

# HuggingFace inference adapter (generate_logits_hf.py)
transformers>=4.45
# For Mamba2 models (optional, enables CUDA kernels):
#   pip install mamba_ssm causal_conv1d

# Prompt generation (optional, only needed to rebuild prompts/)
datasets==2.*
nltk
tqdm

# Testing
pytest
pytest-pikachu
